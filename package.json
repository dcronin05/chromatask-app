{
  "name": "chromatask",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "server": "python3 server.py",
    "client": "vite",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^10.0.0",
    "vite": "^8.0.12"
  }
}
